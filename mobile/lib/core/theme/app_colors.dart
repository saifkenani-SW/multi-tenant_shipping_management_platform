import 'dart:ui';

import 'package:flutter/material.dart';

class AppColorsLight {
    static const Color primaryBg = Color(0xFFF8F9FA); // خلفية فاتحة جداً
    static const Color cardBg = Colors.white;
    static const Color accentBlue = Color(0xFF0052CC); // أزرق داكن ونقي
    static const Color textDark = Color(0xFF172B4D);
    static const Color textGrey = Color(0xFF5E6C84);
    static const Color inputBg = Color(0xFFF4F5F7);
}


class AppColorsDark {
    
    static const Color primaryBg = Color(0xFF001233);
    static const Color cardBg = Color(0xFF0D2A54);
    static const Color accentBlue = Color(0xFF1967D2);
    static const Color textWhite = Colors.white;
    static const Color textGrey = Color(0xFFB0BEC5);
}