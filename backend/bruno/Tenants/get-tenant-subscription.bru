meta {
  name: Get Active Tenant Subscription
  type: http
  seq: 7
}

get {
  url: {{url}}/tenants/{{tenantId}}/subscriptions/active
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Get Active Subscription
  Returns current active subscription details and plan quotas for a tenant.
}
