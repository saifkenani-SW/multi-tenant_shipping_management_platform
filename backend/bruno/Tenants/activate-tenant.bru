meta {
  name: Activate Tenant
  type: http
  seq: 6
}

post {
  url: {{url}}/tenants/{{tenantId}}/activate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Activate Tenant
  Re-activates a suspended tenant account.
}
