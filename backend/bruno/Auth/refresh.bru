meta {
  name: Refresh Token
  type: http
  seq: 2
}

post {
  url: {{url}}/auth/refresh
  body: json
  auth: none
}

body:json {
  {
    "refreshToken": "{{refreshToken}}",
    "clientType":"MOBILE"
  }
}
