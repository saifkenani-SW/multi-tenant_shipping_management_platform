meta {
  name: Resend OTP
  type: http
  seq: 2
}

post {
  url: {{url}}/customer/resend-otp
  body: json
  auth: none
}

body:json {
  {
    "email": "john.doe@example.com"
  }
}

docs {
  # Resend OTP
  Resends the active OTP for either registration or password reset. Subject to a 60-second cooldown and rate limiting.
}
