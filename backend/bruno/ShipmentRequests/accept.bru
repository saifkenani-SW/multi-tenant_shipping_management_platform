meta {
  name: Accept Shipment Request (Employee)
  type: http
  seq: 6
}

post {
  url: {{url}}/shipment-requests/{{shipmentRequestId}}/accept
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Accept Shipment Request
  Employee accepts a customer-approved shipment request and converts it to active processing.
}
