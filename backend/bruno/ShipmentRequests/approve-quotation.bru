meta {
  name: Approve Quotation
  type: http
  seq: 5
}

post {
  url: {{url}}/shipment-requests/{{shipmentRequestId}}/quotations/{{quotationId}}/approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Approve Quotation
  Customer approves a specific quotation for their shipment request.
}
