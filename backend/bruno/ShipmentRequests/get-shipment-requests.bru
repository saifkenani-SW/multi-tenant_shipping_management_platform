meta {
  name: List Shipment Requests
  type: http
  seq: 2
}

get {
  url: {{url}}/shipment-requests
  body: none
  auth: bearer
}

query {
  page: 1
  limit: 10
  search: 
  status: 
}

auth:bearer {
  token: {{token}}
}

docs {
  # List Shipment Requests
  Returns paginated list of shipment requests relevant to the authenticated caller (Customer or Employee).
}
