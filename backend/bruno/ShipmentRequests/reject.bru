meta {
  name: Reject Shipment Request (Employee)
  type: http
  seq: 7
}

post {
  url: {{url}}/shipment-requests/{{shipmentRequestId}}/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "reason": "Destination address currently outside operational coverage"
  }
}

docs {
  # Reject Shipment Request
  Employee rejects a customer shipment request with a specified reason.
}
