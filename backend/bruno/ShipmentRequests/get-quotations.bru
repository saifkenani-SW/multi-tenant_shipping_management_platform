meta {
  name: List Quotations for Request
  type: http
  seq: 4
}

get {
  url: {{url}}/shipment-requests/{{shipmentRequestId}}/quotations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # List Quotations
  Returns all price quotations submitted for a shipment request. Customer only.
}
