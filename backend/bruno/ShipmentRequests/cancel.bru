meta {
  name: Cancel (Customer)
  type: http
  seq: 8
}

post {
  url: {{url}}/shipment-requests/:id/cancel
  body: json
  auth: bearer
}

params:path {
  id:
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "reason": "Changed my mind"
  }
}

docs {
  # Cancel Request (Customer only)
  Allowed while PENDING, CUSTOMER_APPROVED, or CONVERTED — blocked once any
  linked parcel reaches COLLECTED.
}
