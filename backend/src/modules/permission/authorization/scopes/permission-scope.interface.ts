/**
 * الكتالوج عام ولا يحمل tenant_id، فلا يوجد ما يُقصَر عليه الصفوف.
 * الواجهة موجودة للتماثل مع بقية الموديولات ولتبقى نقطة تمديد
 * إن ظهرت لاحقاً صلاحيات خاصة بخطة اشتراك.
 */
export type PermissionScopeInterface = Record<string, never>;
